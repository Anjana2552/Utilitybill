-- MySQL Database Setup for Utility Bill Application
-- Run this script in MySQL Workbench or MySQL Command Line

-- Create the database
CREATE DATABASE IF NOT EXISTS utility_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE utility_db;

-- Show confirmation
SELECT 'Database utility_db created successfully!' AS message;
